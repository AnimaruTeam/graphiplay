export const DEFAULT_URL = "https://countries.trevorblades.com/graphql"

/** Key under which a workspace (tabs, collections, schema cache) is stored. */
export const normalizeEndpointUrl = (url: string) => url.trim()
